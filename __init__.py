"""
Step One:
    put this entire folder into this path
        documents > maya > 2019 > scripts

Step Two:
    open the script editor

Step Three:
    Paste the code below into a Python tab (NOT THE MEL ONE!! IT WONT WORK)

from Anim_Timeline import UI, constants, timeLine, objects
reload(UI)
reload(constants)
reload(timeLine)
reload(objects)
UI.start()

Step Four:
    Press the double triangles at the top of the screen -> UI should appear

"""
